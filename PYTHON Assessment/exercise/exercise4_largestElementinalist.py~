
input_str = input("Enter elements of the list: ")
user_list = [int(x) for x in input_str.split()]

# Find the largest element in the list
largest_element = max(user_list)

print("Largest element:", largest_element)
